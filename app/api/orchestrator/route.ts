import {NextResponse} from 'next/server'; import OpenAI from 'openai'; const o=new OpenAI({apiKey:process.env.OPENAI_API_KEY}); export async function POST(req:Request){try{const{command}=await req.json(); const c=await o.chat.completions.create({model:'gpt-4-turbo-preview',messages:[{role:'system',content:'Orquestrador Brutal Rogger'},{role:'user',content:command}]}); return NextResponse.json({status:'success',message:c.choices[0].message.content});}catch(e){return NextResponse.json({status:'error'},{status:500})}}